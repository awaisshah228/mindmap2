"use client";

import EditorLayout from "@/components/layout/EditorLayout";

/** Editor (canvas) home — no AI panel. Use /ai-diagram to open the AI panel. */
export default function EditorPage() {
  return <EditorLayout />;
}
